import React from 'react';
import ScheduleHome from './src/ScheduleHome/ScheduleHome';

const App = () => {
  const isHorizonal = true;
  return <ScheduleHome isHorizonal={isHorizonal} />;
};

export default App;
